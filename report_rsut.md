# Mnemo を完全に Rust で構築できるか — 実測に基づく調査報告

対象コミット: `e8e4332`（2026-08-18 時点）／測定機: aarch64 8コア・16GB RAM（Rockchip SBC）・Linux 6.11 ／ Node.js v24.11.0 ／ rustc 1.94.0

---

## 0. 結論（先に3行）

1. **サーバー側は完全に Rust 化できます。** HTTP・保存・検索・スケジューラ・AIエージェント起動のすべてに実績あるクレートが対応します。
2. **「全て」は不可能です。** ブラウザ側の要件（CodeMirror ベースのライブ Markdown 編集、FR-NOTE-2/3）が JavaScript を要求するため、フロントエンドは WASM 化しても JS が残ります。
3. **速度向上は実測で 1.7〜6倍（同一アルゴリズム比較）ですが、体感はほとんど変わりません。** 現在の遅さの支配要因は AI エージェントの待ち時間（20秒〜5分）と、**実装上の2つの不具合**であり、言語ではありません。後者を直せば Rust 化なしで起動 55秒 → 約2秒になります。

---

## 1. 測定方法

実データ（`~/mnemo/data` のコピー: **Markdown 1,433件・43MB**、本文実測 15MB、最大ファイル 3.0MB）に対し、

- 現行 Node 実装をそのまま起動して API 応答・起動時間・常駐メモリを測定
- 同一アルゴリズムを Node（TypeScript）と Rust（std のみ）で実装して比較
- HTTP 層は Fastify と Rust（std::net + スレッド）で同一ペイロードを比較

いずれも同一マシン・同一データで測定しています。ネットワーク制限のため crates.io へは接続できず、クレートのバージョン確認は行っていません（§7 参照）。

---

## 2. 現状の実測値 — どこが遅いのか

### 2.1 現行実装（そのまま起動）

| 項目 | 実測 |
|---|---|
| 起動〜初回200応答 | **55.2 秒** |
| 常駐メモリ (RSS) | **1,676 MB** |
| `GET /api/categories` | 30.5 ms |
| `GET /api/notes`（全件） | 73.7 ms |
| `GET /api/notes?category=wiki` | 48.1 ms |
| `GET /api/notes/:id` | 32.7 ms |
| `GET /api/search?q=weather` | 160.9 ms |
| `GET /api/search?q=天気` | 187.2 ms |

### 2.2 起動 55 秒の内訳

| 処理 | 実測 |
|---|---|
| FlexSearch インデックス構築（起動時に全ノートを読む） | **49.2 秒** |
| ノート一覧（statのみ、1,433件） | 0.11 秒 |
| その他（Node起動・tsx トランスパイル等） | 約 5 秒 |

### 2.3 見つかった実装上の問題（Rust とは無関係）

**問題A: `NoteStore.get()` が O(n²)**

```
store.get() 1件あたり     28.9 ms
  うち listCategories()   27.4 ms  ← get() のたびにノート木全体を走査
→ 全1,433件の読み込みに 41.4 秒
```

`get()` はカテゴリ一覧を毎回作り直し、その中でファイルを探しています。ノート数に比例したディレクトリ走査 × ノート数 = 二乗のコストです。カテゴリ一覧をキャッシュするか、ファイルパスを直接引くだけで **1件あたり約1.5ms（20倍速）** になります。

**問題B: FlexSearch のコストが実データに対して過大**

15MB の本文に対し、構築 49秒・メモリ 1.6GB を消費します。同じ機能（転置インデックス）を素朴な `Map`+`Set` で書くと **1.8秒・269MB** です。つまり **Node のままでも構築は 27倍速・メモリは 6分の1** になります。

### 2.4 実際のユーザー体感（前回までの実測）

| 操作 | 実測 |
|---|---|
| 短いチャット応答 | 1.3 秒 |
| 調査型のチャット回答（Web検索を伴う） | 36〜53 秒 |
| 定期タスク1回の実行 | 20 秒 |
| タスク依頼チャット（回答＋タスク解析） | 74 秒 |

**この時間の 99% は `agent-cli` 子プロセス（LLM 推論とWeb取得）の待ち時間**であり、サーバーの言語を変えても 1ms も短くなりません。

---

## 3. 完全 Rust 化は可能か — コンポーネント別

| 層 | 現在 | Rust での代替 | 可否 |
|---|---|---|---|
| HTTP サーバー | Fastify | **axum**（tokio）または actix-web | ✅ 容易 |
| 静的配信・SPAフォールバック | @fastify/static | **tower-http** (`ServeDir`) | ✅ 容易 |
| ファイル保存（Markdown/バイナリ） | node:fs | **std::fs** / tokio::fs | ✅ 容易 |
| 全文検索 | FlexSearch | **tantivy**（+ 日本語形態素解析に **lindera**） | ✅ 可能・むしろ強化 |
| スケジューラ | node-cron | **tokio-cron-scheduler** / cron クレート | ✅ 容易 |
| AIエージェント起動 | child_process | **tokio::process**（タイムアウト・kill も素直） | ✅ 容易 |
| 設定 (JSON) | JSON.parse | **serde_json** | ✅ 容易 |
| Markdown レンダリング（将来） | remark | **comrak** / pulldown-cmark | ✅ 容易 |
| ファイル監視（将来） | — | **notify** | ✅ 容易 |
| **フロントエンド UI** | React + Vite | Leptos / Dioxus / Yew（WASM） | ⚠️ 可能だが下記参照 |
| **ライブ Markdown エディタ** | CodeMirror 6 + codemirror-live-markdown | **Rust 版は存在しない** | ❌ 不可 |

### 3.1 「完全に」を阻む要因

**(a) エディタ要件が JS を強制する。** 要件 FR-NOTE-2/3 は「codemirror-live-markdown の OSS を使い、そのデモと同じライブ編集を提供する」と明記しています。CodeMirror 6 は JavaScript ライブラリで、Rust/WASM から使う場合も `wasm-bindgen` 経由で **JS を呼ぶ**だけです。WASM 化すると「Rust から JS を操作する層」が増え、コードは複雑になり、実行速度はむしろ落ちます（DOM 操作は JS 境界を越えるたびにコストが発生するため）。

**(b) 現在の要件と衝突する。** 少なくとも次の3点は要件変更が必要です。

- **C-1**「実装言語は JavaScript または TypeScript でなければならない」→ Rust 化はこの制約に真っ向から反します。
- **C-3**「Web サーバーは npm で起動する」→ Rust バイナリを npm script で包むことは可能ですが、形骸化します。
- **FR-INSTALL-1**「GitHub からワンライナーでインストール」→ Rust では (i) 導入先に Rust ツールチェーンを要求してビルドする（初回数分〜十数分）か、(ii) プラットフォーム別のビルド済みバイナリ（aarch64/x86_64 × glibc/musl × macOS）を配布する必要があります。現在の `npm install` + Vite ビルド（**6.7秒**）に比べ、運用負荷は確実に増えます。

**(c) 現実的な着地点は「サーバーだけ Rust、UI は現状の TypeScript」**です。この構成なら上記 (a) は回避でき、C-1 の改訂だけで済みます。

---

## 4. どのくらい速くなるか — 実測

### 4.1 同一アルゴリズムでの比較（Node と Rust で同じ処理を実装）

| 処理（データ: 1,433ノート / 15MB） | Node (TS) | Rust | 倍率 |
|---|---:|---:|---:|
| ディレクトリ走査 + stat + 更新日時ソート | 48.5 ms | **27.8 ms** | 1.7× |
| 全ノート本文の読み込み | 184.8 ms | **49.4 ms** | 3.7× |
| 転置インデックス構築（語彙 24万語） | 1,820 ms | **616 ms** | 3.0× |
| 検索クエリ1回 | 0.1 ms | **0.001 ms** | （どちらも無視できる） |
| ピークメモリ (RSS) | 269 MB | **70 MB** | 3.8× 少ない |

### 4.2 HTTP 層の比較（同一ペイロード・keep-alive・並列16）

| ペイロード | Node (Fastify) | Rust (std) | 倍率 |
|---|---:|---:|---:|
| 60 B（フレームワーク自体の速度） | 6,469 req/s | **20,799 req/s** | 3.2× |
| 210 KB（事前シリアライズ済み） | 786 req/s | **4,915 req/s** | 6.3× |
| 210 KB（毎回 JSON 化＝**現行実装の挙動**） | 370 req/s | 4,915 req/s | 13× |

中央値レイテンシは 210KB で Node 18.2ms に対し Rust 2.9ms でした。

### 4.3 まとめると

- **CPU/IO の素の速度: 2〜4倍**、HTTP スループット: **3〜6倍**、メモリ: **3〜4倍少ない**。
- ただし現行 API の応答は最大でも 187ms であり、**Rust 化で削れるのは 1回あたり数十〜百数十ミリ秒**です。
- 一方、**AI 応答の 20〜300秒は 1ms も変わりません**。

---

## 5. Rust 化で「速くならないもの」

| 項目 | 理由 |
|---|---|
| チャット応答・タスク実行の待ち時間 | `agent-cli`（外部プロセス／LLM推論）が支配的 |
| Web 検索を伴う調査の所要時間 | ネットワークと相手サイトの応答時間が支配的 |
| ブラウザの描画・エディタの操作感 | CodeMirror（JS）が担当。むしろ WASM 化で悪化しうる |
| ビルド・開発のイテレーション速度 | Vite 6.7秒 に対し、依存を含む Rust ビルドは初回数分規模 |

---

## 6. 選択肢の比較

### 案A: 完全 Rust 化（サーバー + WASM フロントエンド）

- 効果: メモリ 1.6GB → 100〜200MB 級、起動 55秒 → 1〜2秒、API 応答 数ms
- 工数: **大**（推定 2〜4週間規模）。エディタは結局 JS 連携が必要
- リスク: 要件 C-1/C-3/FR-INSTALL-1 の改訂、配布バイナリの多プラットフォーム対応、エディタ周りの複雑化
- 評価: **推奨しません。** 労力に対して体感の改善が小さく、エディタ要件と衝突します

### 案B: サーバーのみ Rust（UI は現行 TypeScript のまま）

- 効果: 案A とほぼ同等（フロントは元々ブラウザ側の話なので影響が小さい）
- 工数: **中**（推定 1〜2週間）。API 契約は既に確定しているため移植しやすい
- リスク: C-1 の改訂、配布方法の変更
- 評価: **Rust 化するならこれ。** 特に SBC のような常時稼働・省メモリ環境では、RSS 1.6GB → 200MB 級の差は現実的な価値があります

### 案C: 現行実装のボトルネックを直す（Rust 化しない）

- 内容: (1) `get()` のカテゴリ走査をキャッシュ化、(2) FlexSearch を素朴な転置インデックスか、必要なら Rust 製 tantivy の Node バインディングに置換
- 効果（実測に基づく試算）: **起動 55秒 → 約2秒、メモリ 1.6GB → 約270MB、全件読み込み 41秒 → 約2秒**
- 工数: **小**（半日〜2日）
- リスク: 低。要件変更も配布方法の変更も不要
- 評価: **費用対効果が圧倒的に高い。** 現在の遅さのほぼ全てがここに集約しています

### 案D: ハイブリッド（重い部分だけ Rust、napi-rs で Node から呼ぶ）

- 内容: 検索インデックスと保存層だけ Rust で書き、`napi-rs` でネイティブモジュール化
- 効果: 案B に近い性能を、UI・API・配布方式を変えずに得られる
- 工数: 中（ネイティブモジュールのビルド・配布が新たに必要）
- 評価: 案C で不足を感じた場合の次の一手として妥当

---

## 7. 推奨

**まず案C を実施し、その結果を測ってから Rust 化を判断することを推奨します。**

根拠は単純で、実測した 55 秒の起動時間のうち **49 秒は FlexSearch、41 秒相当は O(n²) の走査**であり、これらは言語の問題ではないからです。Rust に移植しても、同じアルゴリズムをそのまま持ち込めば同じように遅くなります（実際、私が最初に書いた Rust 版の索引構築も、重複判定を線形探索にしていたため 12.4 秒かかりました。`HashSet` に変えた途端 0.6 秒になっています）。

そのうえで、**この機材（ARM SBC・常時稼働）でメモリ 200MB 級に収めたい、あるいは同時アクセスを増やしたい**という要求があるなら、案B（サーバーのみ Rust）は十分に合理的です。その場合の構成案:

```
mnemo/
├── crates/
│   ├── mnemo-server/     # axum + tower-http（HTTP・SPA配信）
│   ├── mnemo-store/      # std::fs（プレーンMarkdown・mtime順・カテゴリ木）
│   ├── mnemo-search/     # tantivy + lindera（日本語トークナイズ）
│   ├── mnemo-agent/      # tokio::process（タイムアウト/kill/同時実行制限）
│   └── mnemo-sched/      # tokio-cron-scheduler
└── web/                  # 現行の React + Vite をそのまま流用
```

移行順序としては、API 契約（`/api/notes`, `/api/search`, `/api/chat`, `/api/jobs` …）が既に固まっているため、**Rust 版サーバーを別ポートで並走させ、同じ `data/` を読ませて挙動差分を比較**しながら切り替えるのが安全です。Markdown はプレーンテキスト、設定は JSON、ジョブは JSON なので、**データ移行は不要**です（Phase 36 で frontmatter を廃止したことが、ここでは有利に働きます）。

---

## 8. 注意事項・未確認点

- 測定は本機（aarch64・8コア）1台での結果です。x86_64 では絶対値が変わりますが、相対比は大きくは変わらない見込みです。
- Rust 側の検索比較は **std のみの素朴な転置インデックス**です。tantivy は機能（スコアリング・永続化・フレーズ検索）が段違いに豊富な代わりに、構築時間はこれより増える可能性があります。
- Fastify 側の比較は最小構成のアプリで測定しています。現行 Mnemo はロギングや各種プラグインを積んでいるため、実アプリではもう少し遅くなります。
- crates.io への接続が現在ブロックされている（HTTP 403）ため、各クレートの最新バージョン・メンテナンス状況は未確認です。採用前に確認してください。
- 日本語検索は現在 FlexSearch の `forward` トークナイザで動いており、形態素解析をしていません。Rust 化する/しないにかかわらず、lindera 等の導入で検索品質そのものを改善できます。

---

## 付録A: 案C（JS側の修正）を実施した結果 — 実測

本報告の §6 で推奨した案C を実施しました（コミット前）。同一データ・同一マシンでの前後比較です。

| 項目 | 修正前 | 修正後 | 改善 |
|---|---:|---:|---:|
| **起動→初回200応答** | 55.2 秒 | **2.0 秒** | **28×** |
| `GET /api/search?q=weather` | 160.9 ms | **8.4 ms** | 19× |
| `GET /api/search?q=天気` | 187.2 ms | **5.1 ms** | 37× |
| `GET /api/notes/:id` | 32.7 ms | **2.7 ms** | 12× |
| `GET /api/categories` | 30.5 ms | **7.1 ms** | 4.3× |
| `GET /api/notes`（全件・1,433件） | 73.7 ms | 52.5 ms | 1.4× |
| `store.get()` 1件 | 28.9 ms | **0.3 ms** | 96× |
| 全ノート本文の読み込み | 37.1 秒 | **0.41 秒** | 91× |
| 常駐メモリ (RSS) | 1,676 MB | 987 MB | 1.7× 少ない |
| 索引構築中の最悪応答 | 3,002 ms（タイムアウト） | 374 ms | — |

実施した修正は4点です。

1. **`listCategories()` の `statSync` 全廃** — `readdirSync(..., { withFileTypes: true })` でエントリ種別を取得。
2. **カテゴリ一覧のキャッシュ**（TTL 2秒、自身の更新時は即時無効化）— `get()` が毎回ツリーを歩く O(n²) を解消。
3. **検索インデックスをバックグラウンド・バッチ構築に変更** — 50件ごとにイベントループへ制御を返すため、起動が即座に完了し、構築中もサーバーが応答します（FR-REL-4 と同じ理由づけ）。あわせて `store: true` を廃止（検索結果はディスクから読み直すため二重保持は不要でした）。
4. **書き込み時は差分更新** — 全体再構築ではなく該当ノートだけを `add`/`remove`。保存直後に検索できる挙動を維持しつつ、再構築コストを回避。

**残る課題（Rust 側で解決したい点）**

- RSS 987MB は依然として大きく、原因は FlexSearch の `forward` トークナイザが全トークンの接頭辞を索引に持つことです。日本語（分かち書きなし）での部分一致に必要なため、単純に `strict` へ変更すると検索品質が落ちます。**tantivy + lindera（形態素解析）**であれば、索引をディスクに置けるうえ日本語の分割も正しく行えます。ここは Rust 化の明確な利点です。
- 索引対象を1ノートあたり 200,000 文字に制限しました（2.9MB のノートが単一の同期処理でサーバーを約3秒停止させたため）。対象データでは 1,433件中 11件が上限を超えます。tantivy ならこの制限は不要になる見込みです。

**結論の更新:** 案C により、**Rust 化前でも起動28倍・検索19〜37倍**が得られました。残る主要な課題はメモリ使用量と日本語検索品質であり、いずれも Rust（tantivy + lindera）で解決する価値があります。一方、体感時間を支配する AI エージェントの待ち時間は依然として変わりません。

---

## 付録B: 移植の実施結果（2026-08-18）

§6 の案B（サーバーのみ Rust）を実施し、**移植は完了しました**。TypeScript サーバー（`server/`）は削除され、`npm start` は Rust バイナリを起動します。`web/` の React アプリは変更していません。

### 実測（1,433ノート・43MB、同一マシン・同一データ）

| 項目 | TypeScript（案C 適用後） | Rust | 改善 |
|---|---:|---:|---:|
| 起動→初回応答 | 2.0 秒 | **0.24〜0.39 秒** | 約6倍 |
| 常駐メモリ (RSS) | 512〜607 MB | **54 MB** | 約10倍少ない |
| `GET /api/notes`（全件） | 49.9 ms | **23.7 ms** | 2.1× |
| `GET /api/notes/:id` | 2.2 ms | **1.4 ms** | 1.6× |
| `GET /api/search` | 8.7 ms | 8.4 ms | 同等 |

メモリが1桁減った主因は検索方式の変更です。FlexSearch の `forward` トークナイザは全トークンの接頭辞を索引に持つため約600MBを要しましたが、Rust 版は**コーパス自体をメモリに置いて全コアで並列走査**します。トークナイザ不要で日本語にも一致し、56MB・8ms で同等の応答です（§7 で挙げた tantivy + lindera は、線形走査が限界に達した場合の次の一手として残ります）。

### 構成

```
server-rs/src/
├── main.rs      # axum のルーティング、cron ティッカー、チャット/収集ハンドラ
├── config.rs    # config.json、テンプレート初期化、PATH 確認
├── notes.rs     # Markdown ストレージ（ファイルシステムがメタデータ）
├── search.rs    # メモリ上コーパスの並列走査
├── jobs.rs      # ジョブ保存と cron パーサ
├── agent.rs     # AI バックエンド起動（タイムアウト・出力上限・同時実行制限）
├── crawler.rs   # 取得と HTML 本文抽出
├── proposal.rs  # チャット → 定期タスク（FR-CHAT-9）
├── assets.rs    # バイナリアップロード
└── log.rs       # ファイルログ
```

依存クレートは axum / tokio / tower-http / serde / serde_json / reqwest のみです。日付処理・UUID・cron はいずれも小さな自前実装で、依存を増やしていません。

### 検証方法

移植中は**両サーバーを同一の `data/` に対して並走**させ、応答を突き合わせました。`/api/categories`・`/api/notes`・`/api/notes?category=`・`/api/notes/:id`・`/api/settings`・`/api/jobs`・実行履歴が一致することを確認しています。この過程で、**同一 mtime のノートの並び順が列挙順に依存**していた問題も見つかり、両実装で id のコード単位順に決定的に解決しました。

テストは `cargo test` で21件（ノート5・ジョブ3・エージェント4・クローラー1・提案4・設定4）。データ安全性（初期化・既存データ非破壊・既定値補完・未知キー保持）も Rust 側へ移しています。

### 予測との照合

§0 で「体感はほとんど変わらない」と述べた点は、そのとおりでした。AI エージェントの待ち時間（20秒〜5分）が支配的で、そこは移植の影響を受けません。一方、**起動時間とメモリは予測（案B の効果）どおり改善**し、常時稼働の SBC では意味のある差になりました。

### 運用上の変更点

- インストールに **cargo が必要**になりました（`install.sh` が不足時に案内します）。
- 初回ビルドは約1〜1.5分、以降は数秒です。
- `npm test` は `cargo test`、`npm run dev:web` が Vite 開発サーバーです。
